function CountdownTimer(a, b) {
    this.initialize.apply(this, arguments)
}

function CDT() {
    var a = new Date("Sat, 1 Jul 2017"),
        b = new CountdownTimer("CDT", a);
    b.countDown()
}
CountdownTimer.prototype = {
    initialize: function(a, b) {
        this.elem = document.getElementById(a), this.tl = b
    },
    countDown: function() {
        var a = "",
            b = new Date,
            c = this.tl - b,
            d = Math.floor(c / 864e5);
            var end = (d + "").slice(-1);
            var result = "";
            if (end === '1' && d !== 11) {
                result = "день";
            } else if ((end === '2' || end === '3' || end === '4') && (d !== 12 && d !== 13 && d !== 14)) {
                result = "дня";
            } else {
                result = "дней";
            }
        return c > 0 ? (a += '<span class="number-wrapper"><div class="line"></div><span class="number day">' + d + "</span><span class='dot-time'></span></span><div class='days-result'>" + result + "</div>", this.elem.innerHTML = a, tid = setTimeout(function(a) {
            return function() {
                a.countDown()
            }
        }(this), 10), void 0) : void(this.elem.innerHTML = '<span class="number-wrapper"><div class="line"></div><span class="number end">Прием заявок окончен!</span></span>')
    },
    addZero: function(a) {
        return ("0" + a).slice(-2)
    }
}, window.onload = function() {
    CDT()
};