<!-- # выписываем данные из ТЗ в массивы -->

<!-- # функция createData(), генерирует n-ое (DATA_AMOUNT) количество объектов
> использует массив данных из вне

Цикл до DATA_AMOUNT заполняет объект данными

> возвращает массив объектов

# функция createPin(), генерирует DOM элемент
> принимает объект из __createData()__

Клонируем разметку map__pin
Заполняем данными из объекта

> возвращает DOM элемент

# функция insertPins(), генерирует DOM элементы и вставляет в разметку

> принимает данные из __createData()__

Создаем Фрагмент
Создаем метки через цикл в количестве от длины массива возвращенной функции __createData()__ через __createPin()__
и добавляем их во Фрагмент
Добавляем Фрагмент в разметку

# функция createCard(), генерирует DOM элемент
> принимает объект из __createData()__

Клонируем разметку map__card
Заполняем данными из объекта

> возвращает DOM элемент

# функция insertCard(), генерирует DOM элемент и вставляет в разметку

> принимает данные из __createData()__

Создаем Фрагмент
Создаем метку из первого объекта массива данных (__createData()__) через __createPin()__
и добавляет во Фрагмент
Добавляем Фрагмент в разметку -->


<!-- # функция рандомной сортировки массива -->

<!-- # функция рандомной целового числа в диапазоне от мин до макс включительно -->

<!-- # функция получения рандомного элемента из массива -->

<!-- # функция которая принимает на вход число и выдает рандомное число от 1 до этого числа, но если на вход 100 то выдает нуль (не для гостей) -->

<!-- # функция которая получает массив, трясет его, рандомно выбирает число из длины массива, создает новый массив путем добавления элементов из исходного массива в количестве полученного рандомного числа -->

<!-- # функция переводящая из английского offer.type в русский -->






